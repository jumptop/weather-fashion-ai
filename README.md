# weather-fashion-ai

This project takes the user's location, preferred fashion styles, colors, brands, and other preferences as input. AI then recommends clothing based on these inputs and generates images of the suggested outfits.

add .env file
